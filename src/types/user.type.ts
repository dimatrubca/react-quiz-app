export declare module User {
    export type CreateUser = {
        name: string,
        surname: string
    }
    export type User = {
        name: string;
        surname: string;
        id: number;
    }
}